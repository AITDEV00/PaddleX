# Shared API-compatibility dependencies — installed in BOTH backends.
# The HPS Docling API layer depends on these regardless of whether layout
# detection runs in-process (direct) or via a Triton server (triton).
#
# These are installed on top of the paddle base image (which provides
# paddlepaddle-gpu, paddlex, numpy, pillow, opencv, pydantic, etc.).
#
# Pinned to the exact versions validated in the running container.

docling-slim==2.114.0
docling-core==2.87.1
fastapi==0.116.1
granian==2.7.9
pydantic==2.10.6
pydantic-settings==2.14.2
pydantic_core==2.27.2
starlette==0.47.2
python-multipart==0.0.32
httpx==0.28.1
pluggy==1.6.0
uvicorn==0.35.0
orjson>=3.10.0